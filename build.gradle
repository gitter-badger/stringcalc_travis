repositories {
    mavenCentral()
}

apply plugin: "java"

dependencies {
    compile 'junit:junit:4.12'
    compile 'org.apache.commons:commons-lang3:3.0'
}

sourceSets {
    main.java.srcDir "src/main"
}

task stage(dependsOn: ['clean'])

//  jar {
//    manifest.attributes "Main-Class": "is.ru.stringcalculator.Calculator"
// }
